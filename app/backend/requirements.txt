# --- 1. Core Backend & API ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.7
python-dotenv>=1.0.1
pydantic>=2.0.0             # [BỔ SUNG QUAN TRỌNG] FastAPI cần cái này để định nghĩa Data Model

# --- 2. Frontend ---
streamlit>=1.31.0
requests>=2.31.0
watchdog>=4.0.0             # [BỔ SUNG] Giúp Streamlit chạy mượt hơn trên Cloud

# --- 3. Database ---
neo4j>=5.17.0

# --- 4. AI & Embeddings ---
google-generativeai>=0.4.0
sentence-transformers>=2.4.0
torch>=2.2.0 --index-url https://download.pytorch.org/whl/cpu
# [LƯU Ý]: Dòng torch trên ép buộc cài bản CPU để nhẹ (giảm dung lượng từ 800MB -> 100MB), tránh sập Streamlit Cloud.

# --- 5. Data & Utilities ---
pandas>=2.2.0
numpy>=1.26.0
Pillow>=10.2.0
httpx>=0.27.0               # [BỔ SUNG] Thư viện mạng nền tảng mà Gemini SDK thường dùng