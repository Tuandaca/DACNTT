# Chỉ cho phép (unignore) thư mục app/backend
!app/backend/

# Chặn tất cả mọi thứ ở thư mục gốc
*


# Vẫn nên ignore các file rác trong chính backend (nếu có)
app/backend/__pycache__
app/backend/*.pyc
app/backend/.env